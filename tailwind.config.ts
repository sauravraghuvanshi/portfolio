// Tailwind CSS v4 — design tokens are defined in app/globals.css using @theme
// This config file is preserved for IDE tooling compatibility only.
// See: https://tailwindcss.com/docs/v4-beta

import type { Config } from "tailwindcss";

const config: Config = {
  // v4 auto-detects content — no content array needed
  darkMode: "class",
  theme: {},
  plugins: [],
};

export default config;
